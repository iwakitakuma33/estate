services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - /app/node_modules
    ports: 
      - "3111:3111"
    environment:
      - NODE_ENV=development
    tty: true
